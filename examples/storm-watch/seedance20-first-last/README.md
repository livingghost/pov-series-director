# Seedance 2.0 First/Last Frame: Storm Watch branch

Clip 1, the invitation, prepared for `bytedance/seedance-2.0` on the OpenRouter surface: both inspected boundary frames travel as `frame_images` data URLs, the prompt carries the causal path and every exclusion because no separate exclusion field exists, and the same pass attempts the storm audio with the post stems kept as authority.

This is the branch that gets executed for real. The other two branches document the same clip for materially different operations; this one also carries the run evidence in `result-log.md`.

Contents:

- `director-package.md`: full directing package for the invitation beat;
- `submission-sheet.md`: the exact surface, controls, settings, observed billing and queue behavior, and unresolved facts, dated 2026-08-04;
- `submitted-text.txt`: the literal `prompt` field;
- `request-body.template.json`: the request shape with placeholders for the two data URLs;
- `post-production.md`: the word 「大丈夫」 (proposed), stems, subtitles, repair routes;
- `result-log.md`: the actual submission and its results;
- submitted media: `../media/clip1-start.jpg` and `../media/clip1-end.jpg`, inspected in `../frame-inspection.md`, lineage in `../media/media-log.md`.

The pre-2026-08-04 version of this branch documented BytePlus ModelArk and was never run; it remains in git history as a contrast of the same model on a different surface.
